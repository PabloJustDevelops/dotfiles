# =============================================================================
#  ENV — NVIDIA Optimus (RTX 3060 laptop) + Wayland
#  Basado en CachyOS wiki y JaKooLit
# =============================================================================

# --- NVIDIA específico ---
env = WLR_NO_HARDWARE_CURSORS,1
env = LIBVA_DRIVER_NAME,nvidia
env = GBM_BACKEND,nvidia-drm
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = NVD_BACKEND,direct
env = WLR_DRM_NO_ATOMIC,1

# --- Forzar NVIDIA como principal si solo tienes la dGPU activa ---
# Si tu portátil tiene iGPU + NVIDIA (Optimus), comenta la de abajo
# y usa prime-run para apps concretas.
env = AQ_DRM_DEVICES,/dev/dri/card1

# --- Qt / GTK en Wayland ---
env = XDG_SESSION_TYPE,wayland
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_DESKTOP,Hyprland
env = QT_WAYLAND_DISABLE_WINDOWDECORATION,1
env = QT_QPA_PLATFORM,wayland;xcb
env = GDK_BACKEND,wayland,x11
env = CLUTTER_BACKEND,wayland
env = SDL_VIDEODRIVER,wayland
env = _JAVA_AWT_WM_NONREPARENTING,1

# --- Forzar sincronización con dbus ---
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

# --- XWayland ---
xwayland {
    force_zero_scaling = true
    use_nearest_neighbor = false
}
